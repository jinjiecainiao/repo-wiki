---
description: "Generate structured wiki documentation for the current repository"
---

Invoke the repo-wiki:generate-wiki skill.

The user may provide arguments:
- `--lang <code>`: Documentation language (e.g., `zh-CN`, `en`, `ja`). Default: `en`.

Pass any arguments to the skill as context.
